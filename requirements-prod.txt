# Production requirements - lightweight core dependencies
# Observability - OpenTelemetry (aligned with requirements.txt ~=1.30.0 stack)
opentelemetry-api>=1.30.0
opentelemetry-sdk>=1.30.0
opentelemetry-exporter-otlp>=1.30.0
opentelemetry-exporter-otlp-proto-http>=1.30.0
opentelemetry-semantic-conventions>=0.51b0
opentelemetry-instrumentation-fastapi~=0.51b0

# LLM observability — Langfuse (free Hobby plan supports unlimited prompts)
langfuse>=2.60.0,<3

# Metrics - Prometheus
prometheus-client>=0.19.0

# Core numerical and scientific computing
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0

# HTTP requests
# SECURITY: upgraded to >=2.32.0 for CVE fixes (latest stable is 2.32.x)
requests>=2.32.0
httpx>=0.25.0

# YAML parsing
pyyaml>=6.0

# Type checking
typing-extensions>=4.0.0

# Logging and monitoring
structlog>=21.0.0

# Security
# SECURITY: cryptography >=50.0.0 — fixes CVE-2026-69247 (Bleichenbacher oracle) + earlier CVEs
cryptography>=50.0.0
hvac>=2.0.0

# Password hashing (required - bcrypt replaces SHA-256 for security)
bcrypt>=4.0.0

# JWT token signing (required - no insecure fallback)
# SECURITY: PyJWT >=2.8.0 — fixes CVE-2026-32597 and CVE-2026-48526
PyJWT>=2.8.0,<3

# Data validation
pydantic[email]>=2.0.0
email-validator>=2.0.0

# Database ORM (async SQLite support)
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0

# ETAP Worker API (FastAPI)
# fastapi>=0.135.0 — aligned with requirements.txt (enables starlette 1.x for CVE fixes)
fastapi>=0.135.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
slowapi>=0.1.9
python-dotenv>=1.0.0
# Starlette — aligned with requirements.txt (starlette>=1.3.1 for CVE fixes)
starlette>=1.3.1

# PDF text extraction
# SECURITY: pypdf replaces deprecated PyPDF2 (30+ CVEs fixed). Latest stable: 5.x
pypdf>=4.0.0,<6.0.0
pdfplumber>=0.7.0

# Document processing
python-docx>=0.8.11
openpyxl>=3.0.0

# AI Context Engine (Code RAG)
# SECURITY: chromadb is vulnerable to CVE-2026-45829 (CVSS 9.3, pre-auth RCE /
# code injection). Affects all versions <= 1.5.9. No fix release available yet
# as of 2026-08-03. Monitor: https://github.com/chroma-core/chroma/issues/6717
# Mitigation:
#   1. DO NOT expose chromadb port (8000) to untrusted networks.
#   2. Only bind to localhost / internal network.
#   3. Upgrade to chromadb>=1.5.10 (or whichever release patches the CVE) when
#      upstream publishes a fix.
chromadb>=0.4.24
sentence-transformers>=2.7.0
tree-sitter>=0.21.0
tree-sitter-python>=0.21.0
# SECURITY: nltk — aligned with requirements.txt pin (nltk>=3.8.0,<3.10.0)
nltk>=3.8.0,<3.10.0
# SECURITY: Pygments — aligned with requirements.txt pin (pygments>=2.17.0,<2.20.0)
pygments>=2.17.0,<2.20.0
# SECURITY: setuptools — fixes MANIFEST.in CVE. Max on PyPI is 75.3.4.
setuptools>=70.0.0,<76.0.0

# Additional utilities
tqdm>=4.62.0
beautifulsoup4>=4.10.0
lxml>=6.1.0

# Phase 2: Security & Infrastructure
pyotp>=2.9.0
# WebAuthn / FIDO2 — aligned with requirements.txt pin (webauthn>=2.0.0,<3.0.0)
webauthn>=2.0.0,<3.0.0
redis>=5.0.0

# Retry and fault tolerance (used by core/retry.py)
tenacity>=8.2.0

# API & Database dependencies
alembic>=1.13.0

# Task queue (used by worker/celery_app.py)
celery>=5.3.0

# AI Memory & Graph Database
neo4j>=5.0.0
# LangChain — pinned to match requirements.txt 1.x line (security upgrade).
# Looser bounds than requirements.txt so production can absorb patch releases
# without a re-pin, but still constrained to the 1.x contract.
langchain-core>=1.5.0,<2
langchain-openai>=1.4.0,<2
langchain-community>=0.4.0,<0.5
langchain-experimental>=0.4.0,<0.5
langchain-qdrant>=1.0,<2
langsmith>=0.10.0,<0.11
qdrant-client>=1.7.0

defusedxml>=0.7.1

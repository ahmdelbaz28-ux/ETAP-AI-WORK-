apiVersion: v1
kind: Service
metadata:
  name: etap-api
  namespace: etap
  labels:
    app.kubernetes.io/name: etap
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: etap
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8000"
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: etap
    app.kubernetes.io/component: api
---
apiVersion: v1
kind: Service
metadata:
  name: etap-db
  namespace: etap
  labels:
    app.kubernetes.io/name: etap
    app.kubernetes.io/component: db
    app.kubernetes.io/part-of: etap
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app.kubernetes.io/name: etap
    app.kubernetes.io/component: db
---
apiVersion: v1
kind: Service
metadata:
  name: etap-redis
  namespace: etap
  labels:
    app.kubernetes.io/name: etap
    app.kubernetes.io/component: redis
    app.kubernetes.io/part-of: etap
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    app.kubernetes.io/name: etap
    app.kubernetes.io/component: redis

# =============================================================================
# AhmedETAP — FOSSA Configuration
# =============================================================================
# Documentation: https://docs.fossa.com/docs/fossa-yml-reference
# =============================================================================

version: 3

project:
  name: ETAP-AI-WORK-
  url: "https://github.com/ahmdelbaz28-ux/ETAP-AI-WORK-"

paths:
  exclude:
    - "tests/"
    - "test/"
    - "docs/"
    - "artifacts/"
    - "scratch/"
    - "node_modules/"
    - ".venv/"
    - "venv/"
    - "skills/ppt/"
    - "coverage/"
    - "ts-service/"

targets:
  exclude:
    - path: ts-service
    - path: ts-service/package.json

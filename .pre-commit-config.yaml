repos:
  # ── Stage 1: Code Quality ──────────────────────────────────────────
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        files: ^prompts/.*\.yaml$
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: detect-private-key
<<<<<<< HEAD
      # CRITICAL FIX: Added check-json for .json config file validation.
      # Per CI/CD skill: "Shift Left — catch problems as early in the pipeline as possible."
      - id: check-json
        files: ^config/.*\.json$
=======
>>>>>>> origin/fix/scenario-tests-properly

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.0
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

<<<<<<< HEAD
  # ── Stage 2: Type Checking ───────────────────────────────────────────
  # CRITICAL FIX: Changed from manual-only to pre-push stage.
  # Per CI/CD skill: "No gate can be skipped." Type errors must be caught
  # automatically, not only when developers remember to run them manually.
=======
  # ── Stage 2: Type Checking (manual only — requires pyright installed) ──
>>>>>>> origin/fix/scenario-tests-properly
  - repo: local
    hooks:
      - id: type-check
        name: type check
        entry: pyright
        language: system
        types: [python]
        pass_filenames: false
<<<<<<< HEAD
        stages: [push]
=======
        stages: [manual]
>>>>>>> origin/fix/scenario-tests-properly

  # ── Stage 3: Fast Tests ────────────────────────────────────────────
  - repo: local
    hooks:
      - id: fast-tests
        name: fast tests
        entry: pytest -x -q --timeout=60 -m "not slow"
        language: system
        types: [python]
        pass_filenames: false
        stages: [push]

  # ── Stage 4: Skill Schema Validation ────────────────────────────────
  - repo: local
    hooks:
      - id: validate-skill-schema
        name: validate skill schemas
        entry: python -m pytest tests/property_based/ -x -q --timeout=30
        language: system
        pass_filenames: false
        files: ^skills/.*\.py$

  # ── Stage 5: Prompt Validation ─────────────────────────────────────
  - repo: local
    hooks:
      - id: validate-prompts
        name: Validate YAML Prompt Files
        entry: python3 scripts/validate_prompts.py --strict
        language: system
        files: ^prompts/.*\.yaml$
        pass_filenames: false

  # ── Stage 6: Security Scan ────────────────────────────────────────
<<<<<<< HEAD
  # CRITICAL FIX: Added gitleaks pre-commit hook (better than custom script).
  # Per CI/CD skill: security scans should use the best available tools.
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
=======
>>>>>>> origin/fix/scenario-tests-properly
  - repo: local
    hooks:
      - id: security-scan
        name: Security Scan
        entry: python scripts/security_scan.py
        language: system
        pass_filenames: false
        always_run: true

# Alertmanager configuration for ETAP-AI-WORK
# Defines notification routing, inhibition rules, and receiver configurations
#
# SECURITY NOTE: All credentials (SMTP password, Slack webhook URLs) in this
# file are PLACEHOLDERS. Replace them with real values via:
#   1. Environment variable substitution in your deployment script
#   2. A secret management tool (Vault, AWS Secrets Manager, etc.)
#   3. Kubernetes Secrets (mounted as a volume)
# NEVER commit real credentials to version control.

global:
  # The smarthost and SMTP sender used for notifications.
  # Replace with your SMTP server in production.
  smtp_smarthost: 'localhost:25'
  smtp_from: 'alertmanager@example.org'
  smtp_auth_username: 'alertmanager'
  # SECURITY: Replace 'REPLACE_WITH_SMTP_PASSWORD' via env var or secret.
  # Do NOT use 'password' — it looks like a real (weak) credential.
  smtp_auth_password: 'REPLACE_WITH_SMTP_PASSWORD'

# The root route on which each incoming alert enters.
route:
  # The labels by which incoming alerts are grouped together. For example,
  # multiple alerts coming in for cluster=A and alertname=LatencyHigh would
  # be batched into a single group.
  group_by: ['alertname', 'cluster', 'service']

  # When a new group of alerts is created by an incoming alert, wait at
  # least 'group_wait' to send the initial notification.
  # This way ensures a single alert that frequently changes won't cause a lot of
  # notification spam.
  group_wait: 30s

  # When the first notification was sent, wait 'group_interval' to send a batch
  # of new alerts that started firing for that group.
  group_interval: 5m

  # If an alert has successfully been sent, wait 'repeat_interval' to
  # resend them.
  repeat_interval: 3h

  # A default receiver
  receiver: default

  # All the above attributes are inherited by all child routes and can
  # overwritten on each.

  # The child route trees.
  routes:
    # Send severity=slack alerts to slack.
    - match:
        severity: slack
      receiver: slack_general

    # Send alerts with service=engineering to dedicated receiver
    - match:
        service: engineering
      receiver: engineering_alerts

    # Send alerts with service=etap to dedicated receiver
    - match:
        service: etap
      receiver: etap_notifications

receivers:
  - name: 'default'
    email_configs:
    - to: 'admin@example.org'
      from: 'alertmanager@example.org'
      smarthost: 'localhost:25'
      auth_username: 'alertmanager'
      auth_identity: 'alertmanager'
      # SECURITY: Replace via env var or secret in production.
      auth_password: 'REPLACE_WITH_SMTP_PASSWORD'

  - name: 'slack_general'
    slack_configs:
    - channel: '#alerts'
      text: '{{ template "slack.default.text" . }}'
      api_url: 'https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK'

  - name: 'engineering_alerts'
    slack_configs:
    - channel: '#engineering-alerts'
      text: '{{ template "slack.default.text" . }}'
      api_url: 'https://hooks.slack.com/services/YOUR/ENGINEERING/WEBHOOK'

  - name: 'etap_notifications'
    slack_configs:
    - channel: '#etap-notifications'
      text: '{{ template "slack.default.text" . }}'
      api_url: 'https://hooks.slack.com/services/YOUR/ETAP/WEBHOOK'

# Inhibition rules allow to mute a set of alerts given that another alert is
# firing.
# We use this to mute any warning-level notifications if the same alert is
# already critical.
inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    # Apply inhibition if the alertname is the same.
    equal: ['alertname', 'cluster', 'service']
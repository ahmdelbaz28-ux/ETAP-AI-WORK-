# =============================================================================
# Test Environment — Safe for CI and local testing
# No real secrets — this file IS committed
# =============================================================================

PORT=0
NODE_ENV=test
LOG_LEVEL=error

DATABASE_URL=postgresql://ci_user:ci_password@localhost:5432/testdb
JWT_SECRET=test-jwt-secret-not-for-production

CORS_ORIGIN=http://localhost:3000

# Default values for etap-ai.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  imageRegistry: ""
  imagePullSecrets: []
  storageClass: ""

image:
  repository: etap-ai-platform/engineering-service
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: "etap-ai"

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

<<<<<<< HEAD
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  fsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 1000
=======
podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000
>>>>>>> origin/fix/scenario-tests-properly

# API Service Configuration
api:
  replicaCount: 2
  image:
    repository: etap-ai-platform/engineering-service
    pullPolicy: IfNotPresent
    tag: ""
  service:
    type: ClusterIP
    port: 8000
  ingress:
    enabled: false
    className: ""
    annotations: {}
      # kubernetes.io/ingress.class: nginx
      # kubernetes.io/tls-acme: "true"
    hosts:
      - host: chart-example.local
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls: []
    #  - secretName: chart-example-tls
    #    hosts:
    #      - chart-example.local
  resources:
    limits:
      cpu: 500m
      memory: 1Gi
<<<<<<< HEAD
      ephemeral-storage: "2Gi"
    requests:
      cpu: 250m
      memory: 512Mi
      ephemeral-storage: "512Mi"
  nodeSelector: {}
  tolerations: []
  # SECURITY AUDIT: Pod anti-affinity prevents co-scheduling on same node,
  # ensuring PDB availability guarantee is meaningful.
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/component
                  operator: In
                  values:
                    - api
            topologyKey: kubernetes.io/hostname
  # PodDisruptionBudget — ensures minimum pod availability during voluntary disruptions
  pdb:
    enabled: true
    minAvailable: 1
  # HorizontalPodAutoscaler — auto-scale based on CPU/memory
  # CRITICAL FIX: Enabled HPA for production readiness.
  # Per CI/CD skill: "Smaller batches and more frequent releases reduce risk."
  # HPA ensures the platform can handle load spikes without manual intervention.
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 75
    # targetMemoryUtilizationPercentage: 80
=======
    requests:
      cpu: 250m
      memory: 512Mi
  nodeSelector: {}
  tolerations: []
  affinity: {}
>>>>>>> origin/fix/scenario-tests-properly

# Celery Worker Configuration
worker:
  replicaCount: 2
  image:
    repository: etap-ai-platform/engineering-service
    pullPolicy: IfNotPresent
    tag: ""
  resources:
    limits:
      cpu: 500m
      memory: 1Gi
<<<<<<< HEAD
      ephemeral-storage: "5Gi"
    requests:
      cpu: 250m
      memory: 512Mi
      ephemeral-storage: "1Gi"
=======
    requests:
      cpu: 250m
      memory: 512Mi
>>>>>>> origin/fix/scenario-tests-properly
  nodeSelector: {}
  tolerations: []
  affinity: {}

# Redis Configuration
redis:
  enabled: true
  architecture: standalone
  auth:
<<<<<<< HEAD
    # SECURITY: must be true in production. Set redis.auth.password via
    # `--set redis.auth.password=<strong-password>` or via a sealed Secret.
    enabled: true
    password: ""
=======
    enabled: false
>>>>>>> origin/fix/scenario-tests-properly
  master:
    persistence:
      enabled: true
      size: 8Gi
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi

# Environment Variables
env:
  ENVIRONMENT: production
  PORT: 8000
  REDIS_URL: redis://etap-ai-redis-master:6379/0
  USE_ETAP: "false"
  PRIVACY_MODE: "true"
<<<<<<< HEAD
  # SECURITY: ENGINEERING_SERVICE_API_KEY is injected via secretKeyRef.
  # The secret "etap-api-key" is created BEFORE Helm deployment
  # (see k8s/etap-api-key-secret.yaml). The Helm deployment template
  # references it via secretKeyRef — the value here is ONLY used by
  # the Helm-managed secret template for dev/testing fallback.
  # In production, override with: --set env.ENGINEERING_SERVICE_API_KEY=<key>
  # or provision via sealed-secrets / external-secrets-operator.
  # The empty string ensures Helm won't render a valid-looking default key.
  # A startup check in the app will refuse to start if the key is empty.
  ENGINEERING_SERVICE_API_KEY: ""
=======
  ENGINEERING_SERVICE_API_KEY: "your-api-key-here"
>>>>>>> origin/fix/scenario-tests-properly
  LOG_LEVEL: "INFO"

# Database Configuration
database:
  persistence:
    enabled: true
    size: 10Gi
    storageClass: ""

# Resource limits and requests
resources:
  limits:
    cpu: 1000m
    memory: 2Gi
  requests:
    cpu: 500m
    memory: 1Gi

autoscaling:
<<<<<<< HEAD
  # CRITICAL FIX: Enabled autoscaling for production readiness.
  enabled: true
  minReplicas: 2
  maxReplicas: 10
=======
  enabled: false
  minReplicas: 1
  maxReplicas: 100
>>>>>>> origin/fix/scenario-tests-properly
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

<<<<<<< HEAD
affinity: {}

# Network Policy Configuration (Zero-Trust)
networkPolicy:
  # Set to true to enforce zero-trust network policies
  enabled: true
  # Labels used to identify the Ingress controller pods and namespace
  # Adjust these to match your cluster's Ingress controller deployment
  ingressController:
    namespaceLabels:
      kubernetes.io/metadata.name: ingress-nginx
    podLabels:
      app.kubernetes.io/name: ingress-nginx
=======
affinity: {}
>>>>>>> origin/fix/scenario-tests-properly

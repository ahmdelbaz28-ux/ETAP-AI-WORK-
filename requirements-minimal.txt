# AhmedETAP - Minimal Core Dependencies
# Install with: pip install -r requirements-minimal.txt
# For ML/AI features, also install: pip install -r requirements-ml.txt
#
# NOTE: Versions are pinned with ~= (compatible-release) so that the
# sonarcloud CI job can install with `pip install --only-binary :all:`
# from verified releases (SonarCloud S8544). All pins below have
# CPython 3.12 manylinux wheels.

# Core numerical and scientific computing
numpy>=1.26.0,<2.3.0
scipy~=1.14.1
pandas~=2.2.3

# Visualization
matplotlib~=3.9.4

# Testing
# SECURITY: bumped from 8.3.5 → 9.0.3 to fix CVE-2025-71176 (vulnerable tmpdir handling)
pytest~=9.0.3
pytest-cov~=6.0.0

# HTTP requests
# requests ~= 2.32.x (latest stable; 2.34 was never released)
requests>=2.32.0

# YAML parsing
pyyaml~=6.0.2

# Security
# cryptography>=50.0.0 — fixes CVE-2026-69247 (Bleichenbacher oracle) + earlier CVEs
cryptography>=50.0.0
bcrypt~=4.2.1
# PyJWT >=2.8.0 — fixes CVE-2026-32597 and CVE-2026-48526 (2.13.0 was never released)
PyJWT>=2.8.0,<3

# Data validation
# pydantic >=2.8.0 (2.11.7 was never released; latest stable is 2.10.x)
pydantic>=2.8.0,<3

# Web framework (Engineering Service)
# fastapi>=0.135.0 — aligned with requirements.txt (enables starlette 1.x)
fastapi>=0.135.0
uvicorn~=0.32.1
slowapi~=0.1.10
python-dotenv>=1.1.0
# starlette — aligned with requirements.txt (starlette>=1.3.1)
starlette>=1.3.1

# Database
sqlalchemy[asyncio]~=2.0.36
aiosqlite~=0.20.0

# Logging
structlog~=24.4.0

# Utilities
typing-extensions~=4.12.2
tqdm~=4.67.0
tenacity~=9.0.0
# setuptools — fixes MANIFEST.in CVE (setuptools>=83.0.0)
setuptools>=83.0.0
# Pygments — aligned with pyproject.toml pin (pygments>=2.20.0)
Pygments>=2.20.0

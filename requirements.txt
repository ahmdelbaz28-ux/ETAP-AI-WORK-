# =============================================================================
# AhmedETAP — Pinned Production Requirements
# =============================================================================
# All versions pinned with ~= for patch-level updates only (no breaking changes).
# GUI automation and Windows-only deps moved to [win32] extras in pyproject.toml.
# =============================================================================

# Observability - OpenTelemetry
# Pin all OTel packages to compatible versions. opentelemetry-sdk 1.30.x
# requires opentelemetry-semantic-conventions 0.51b0 — which is compatible
# with opentelemetry-instrumentation-fastapi 0.51b0 (line below).
opentelemetry-api~=1.30.0
opentelemetry-sdk~=1.30.0
opentelemetry-exporter-otlp~=1.43.0
opentelemetry-semantic-conventions~=0.51b0
# OTLP/HTTP exporter for the Langfuse OTLP endpoint (used by core/tracing.py)
opentelemetry-exporter-otlp-proto-http~=1.30.0

# LLM observability — Langfuse (free Hobby plan supports unlimited prompts)
# Used by integrations/langfuse_integration.py and agents/prompt_loader.py.
langfuse~=2.51.0

# LLM SDKs (traced via langfuse.openai / langfuse.anthropic wrappers)
openai~=1.50.0
anthropic~=0.116.0

# Supabase (managed Postgres + Storage + Auth) — used by integrations/supabase_*.py
# Required when deploying to HF Space (where the filesystem is wiped on restart)
# or when using Supabase Storage for PDFs/reports/screenshots.
supabase~=2.31.0

# Neo4j (graph database) — used by integrations/neo4j_integration.py
# Required for network topology modeling and knowledge graph operations.
# NOTE: must be >=5.25.0 to satisfy langchain-neo4j (see requirements-prod.txt)
neo4j>=5.25.0,<7

# Metrics - Prometheus
prometheus-client>=0.19,<0.26

# Core numerical and scientific computing
numpy>=1.26,<2.6
scipy>=1.12,<1.19
pandas~=2.2.0

# Visualization
matplotlib~=3.8.0

# System monitoring
psutil~=5.9.0

# HTTP requests
# SECURITY: upgraded from 2.31.0 → 2.33.0 to fix CVE-2024-35195 (cert
# verification bypass) and CVE-2024-47081.
requests~=2.33.0

# CAD file processing
ezdxf~=1.3.0

# Language detection
langdetect~=1.0.9

# YAML parsing
pyyaml~=6.0.0

# Type checking
typing-extensions>=4.9.0

# Logging and monitoring
structlog~=23.2.0

# Security
# SECURITY: upgraded from 41.0.0 → 48.0.0 to fix 9 CVEs including:
#   CVE-2023-50782 (NULL deref in signature verification)
#   CVE-2024-0727 (PKCS#12 NULL deref)
#   GHSA-537c-gmf6-5ccf (Bleichenbacher timing in PKCS#1 v1.5)
cryptography~=48.0.0
hvac~=2.1.0

# Password hashing
bcrypt~=4.1.0

# JWT token signing
# NOTE: must be >=2.12.0 to satisfy supabase-auth 2.31.0
PyJWT>=2.12.0,<3

# Data validation
# NOTE: pydantic must be >=2.11.7 to satisfy `realtime` (sub-dep of supabase~=2.31.0)
# and <3 to satisfy langfuse/openai/anthropic. Pinning to ~=2.5.0 caused
# ResolutionImpossible at install time.
pydantic>=2.11.7,<3
email-validator>=2.1.0,<3

# Database ORM (async PostgreSQL + SQLite support)
sqlalchemy[asyncio]~=2.0.0
aiosqlite~=0.19.0
asyncpg>=0.29,<0.32

# ETAP Worker API (FastAPI)
# FastAPI: upgraded from 0.109.x → 0.115.4+ to resolve dependency conflict
# with starlette 0.40.x (required for CVE-2024-47874 + CVE-2025-54121 fixes).
# fastapi 0.109.x requires starlette<0.37, but we pin starlette>=0.40 for the
# security fixes. fastapi 0.115.4+ supports starlette>=0.40,<0.42 — compatible
# with our starlette>=0.40.0,<0.41.0 pin below.
# See: https://github.com/fastapi/fastapi/releases/tag/0.115.0
fastapi>=0.115.4,<0.116.0
uvicorn~=0.25.0

# ==========================================
# ETAP User Guide Integration - PDF Processing
# ==========================================
# SECURITY: pypdf upgraded from 5.9.0 → 6.13.3 to fix 30 CVEs including:
#   CVE-2025-55197, CVE-2026-22690, CVE-2026-49461, CVE-2026-54531 (ReDoS / XML parsing)
# Note: project switched from PyPDF2 → pypdf (PyPDF2 is deprecated since 2022).
pypdf~=6.13.0
pdfplumber>=0.10,<0.12

# Text processing and NLP
# SECURITY: nltk upgraded from 3.8.0 → 3.9.4 to fix 13 CVEs including:
#   PYSEC-2024-167 (RCE via pickled models)
#   CVE-2026-33230 / CVE-2026-33231
nltk~=3.9.4

# Document processing
python-docx~=1.1.0
openpyxl~=3.1.0
reportlab~=4.0.0

# Additional utilities
tqdm~=4.66.0
beautifulsoup4~=4.12.0
# SECURITY: lxml upgraded from 5.1.0 → 6.1.0 to fix PYSEC-2026-87 (XXE)
lxml~=6.1.0

# Phase 2: Security & Infrastructure
pyotp~=2.9.0
# WebAuthn / FIDO2 — required for production MFA (HIGH #8 from AhmedETAP_Error_Report_AR.pdf).
# Without this, security/mfa.py rejects all WebAuthn registrations/assertions.
webauthn~=2.0.0
redis~=5.0.0
httpx~=0.27.0

# Phase 5: Kubernetes
kubernetes~=29.0.0

# Phase 3: WebSocket
websockets~=12.0

# MQTT messaging for SCADA integration
paho-mqtt~=1.6.1

# MongoDB (optional — only needed if SCADA MongoDB backend is used)
# pymongo~=4.6.0

# GIS processing (optional — requires GDAL system libraries)
# geopandas~=0.14.0
# shapely~=2.0.0

# Retry and fault tolerance
tenacity>=8.2,<9.2

# API & Database dependencies
alembic~=1.13.0

# Python multipart for FastAPI file uploads
python-multipart~=0.0.6

# Starlette (FastAPI dependency - pinned for compatibility)
# SECURITY: upgraded from 0.35.0 → 0.40.0 to fix CVE-2024-47874 (DoS via
# multipart forms) and CVE-2025-54121 (memory leak). We use 0.40.x because
# it is compatible with fastapi>=0.115.4 (upgraded above). Full upgrade to
# starlette 1.1.0 requires upgrading FastAPI to >=0.118 first (see P1 backlog).
starlette>=0.40.0,<0.41.0

# Celery for async task queue
celery~=5.3.0

# Opentelemetry FastAPI instrumentation
# Must match the opentelemetry-semantic-conventions version above (0.51b0).
opentelemetry-instrumentation-fastapi~=0.51b0

# Production requirements (lightweight subset)
-r requirements-prod.txt

# =============================================================================
# ETAP GUI Agent — Computer Use Agent (CUA) dependencies
# =============================================================================
# These are required for REAL CUA execution (screenshot + click + type).
# On headless servers (HF Space, CI), they are optional — the agent falls back
# to Format U (planning-only response) gracefully.
# =============================================================================

# Google Gemini Vision SDK — visual perception layer for the CUA Loop
# Used by integrations/gemini_vision.py to analyze screenshots and decide
# the next action (click coordinates, text to type, hotkeys to press).
google-generativeai~=0.8.0

# Pillow — image handling for screenshots (also a pyautogui dep, listed explicitly)
# SECURITY: upgraded from 10.2.0 → 12.2.0 to fix CVE-2024-28219 (buffer overflow)
# and CVE-2026-42310.
Pillow~=12.2.0

# OpenCV — local screen analysis (offline fallback when Gemini is unreachable)
# Used by integrations/opencv_vision.py for contour detection + UI element detection
opencv-python~=4.10.0

# (numpy and httpx are already declared above — duplicates removed to avoid
# pip resolver conflicts. numpy>=1.26,<2.6 at line 36, httpx~=0.27.0 at line 106.)

# pyautogui — mouse/keyboard control (desktop only, NOT installable on headless Linux
# without X11 libs). Install manually on the desktop where the CUA will run:
#   pip install pyautogui
# Also requires Tesseract OCR for the optional OCR fallback path:
#   sudo apt-get install tesseract-ocr   (Linux)
#   https://github.com/UB-Mannheim/tesseract/wiki  (Windows)


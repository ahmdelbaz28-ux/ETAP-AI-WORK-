# Observability - OpenTelemetry
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0

# Metrics - Prometheus
prometheus-client>=0.19.0

# Core numerical and scientific computing
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0

# Visualization
matplotlib>=3.4.0

# ETAP COM Automation (Windows only) - moved to optional dependencies
# pywin32>=303; sys_platform == 'win32'

# GUI Automation
pyautogui>=0.9.53
opencv-python>=4.5.0

# System monitoring
psutil>=5.8.0

# HTTP requests
requests>=2.26.0

# Language detection
langdetect>=1.0.9

# YAML parsing
pyyaml>=6.0

# Type checking
typing-extensions>=4.0.0

# Logging and monitoring
structlog>=21.0.0

# Security
cryptography>=36.0.0
hvac>=2.0.0

# Password hashing (required - bcrypt replaces SHA-256 for security)
bcrypt>=4.0.0

# JWT token signing (required - no insecure fallback)
PyJWT>=2.6.0

# Data validation
pydantic>=2.0.0
email-validator>=2.0.0

# Database ORM (async SQLite support)
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0

# ETAP Worker API (FastAPI)
fastapi>=0.104.0
uvicorn>=0.24.0

# ==========================================
# ETAP User Guide Integration - PDF Processing
# ==========================================
# PDF text extraction
PyPDF2>=3.0.0
pdfplumber>=0.7.0

# Text processing and NLP (optional - required for advanced RAG features)
nltk>=3.6.0

# Document processing
python-docx>=0.8.11
openpyxl>=3.0.0
reportlab>=3.6.0

# Additional utilities
tqdm>=4.62.0
beautifulsoup4>=4.10.0
lxml>=4.6.0

# Phase 2: Security & Infrastructure
pyotp>=2.9.0
redis>=5.0.0
httpx>=0.25.0

# Phase 6: ML/AI - Core
scikit-learn>=1.3.0
tensorflow>=2.15.0; python_version >= '3.9'

# Phase 6b: ML/AI - Enhanced Predictive Analytics
prophet>=1.1.0                      # Facebook Prophet - seasonal load forecasting
xgboost>=2.0.0                      # Gradient boosting - fault prediction
shap>=0.43.0                        # Model explainability - SHAP values
optuna>=3.0.0                       # Hyperparameter optimization
pyod>=1.0.0                         # 30+ anomaly detection algorithms

# Phase 6c: ML/AI - Deep Learning & GNN
torch>=2.0.0                        # PyTorch - deep learning foundation
torch-geometric>=2.3.0              # Graph Neural Networks for power grids

# Phase 6d: ML/AI - Model Management
mlflow>=2.0.0                       # Model tracking, versioning, registry

# Phase 6e: ML/AI - NLP & RAG Enhancement
transformers>=4.30.0                # HuggingFace Transformers
sentence-transformers>=2.2.0        # Sentence embeddings for RAG

# Phase 5: Kubernetes
kubernetes>=29.0.0

# Phase 3: WebSocket
websockets>=12.0

# Core requirements for SCADA-GIS integration
paho-mqtt>=1.6.1
# pyScada==0.10.12  # Commented out - not on PyPI
# pyScada-mqtt       # Commented out - not on PyPI
# pyScada-opcua      # Commented out - not on PyPI
# pyScada-modbus     # Commented out - not on PyPI
# pyScada-ems        # Commented out - not on PyPI
pymongo
# arcpy  # Commented out - installed with ArcGIS Pro only
pandas
numpy
requests
flask  # For potential web APIs
geopandas  # For GIS operations
shapely  # For geometric operations

# Retry and fault tolerance (used by core/retry.py)
tenacity>=8.2.0

# API & Database dependencies
alembic

# LangWatch - commented out as it may not be on PyPI
# langwatch>=0.1.0

# Main requirements file
# For production deployments (lightweight): requirements-prod.txt
# For ML/AI features (heavy): requirements-ml.txt
# For development: requirements-dev.txt

-r requirements-prod.txt

# Uncomment the line below to include ML/AI dependencies
# -r requirements-ml.txt

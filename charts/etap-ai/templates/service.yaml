apiVersion: v1
kind: Service
metadata:
  name: {{ include "etap-ai.fullname" . }}
  labels:
    {{- include "etap-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "etap-ai.selectorLabels" . | nindent 4 }}

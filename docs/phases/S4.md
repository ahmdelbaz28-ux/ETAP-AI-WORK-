# S4 — الطوارئ الحقيقي + Stop لكل عملية (بند 5-4)

> **الصق هذا الملف وحده للوكيل المنفذ. توقف عند التقرير.**

## هويتك وقوانينك
R1 لا ادعاء بلا أمر ونتيجة حرفية. R2 تجميد أثناء القياس. R3 لا ادعاء توقف لم يحدث فعلًا. R7 لا ملف خارج القائمة.

## حالة البداية المؤكدة
- `ui/src/components/chat/EmergencyStopButton.tsx` يستدعي حاليًا فقط `checkEmergencyStop` و`activateEmergencyStop("chat_workspace_ui")` — أي **kill-switch الخاص بـCUA** (`GET/POST /admin/cua/kill-switch`) — **ولا يوقف أي دراسة/stream جارٍ**.
- `ui/src/store/chatStore.ts` يملك: `activeChatAbort` (≈سطر 134)، `abortStream()` (≈1009) الذي يستدعي `activeChatAbort.abort(new Error("USER_ABORT"))`، و`activateEmergencyStop` (≈1068).
- اختبارات قائمة: `ui/src/__tests__/p6/chatStore.test.ts` (تشمل emergency stop).

## الملفات المسموح لمسها حصريًا
`ui\src\components\chat\EmergencyStopButton.tsx` · `ui\src\components\chat\ActivityDrawer.tsx` · `ui\src\store\chatStore.ts` (فقط إن لزم ربط — **ممنوع تغيير التوقيعات**) · `ui\src\__tests__\p6\*` · `docs\status\STATUS_BOARD.md`

## الخطوات
1. **اقرأ أولًا** `ActivityDrawer.tsx` و`chatStore.ts` وافحص هل يوجد مسار إلغاء خلفي حقيقي (ابحث في `api/studies.py` وأمثاله عن cancel/abort). **لا تخترع مسارًا لا وجود له.**
2. **زر الطوارئ:** عند الضغط استدعِ `abortStream()` **ثم** `activateEmergencyStop()`، واعرض حقيقتين منفصلتين: «أُوقف محليًا (stream المحادثة)» و«أكده الخادم (kill-switch)». لا تعرض «توقف مؤكد» لشيء لم يؤكده الخادم.
3. **ActivityDrawer:** أضف زر `Stop` لكل عملية جارية يستدعي نفس مسار الإجهاض (أو الـcancel الخلفي إن كان موجودًا ومؤكدًا بكوده).
4. **اختبارات vitest** في `ui\src\__tests__\p6\`:
   - ترتيب الاستدعاء: abort ثم activate.
   - فشل الخادم ⇒ لا رسالة «توقف مؤكد» ويظهر خطأ صادق.
   - الزر يظل معطّلًا بعد التنشيط (السلوك الحالي محفوظ).
5. **وثّق بدقة** في التقرير: ما يوقفه هذا الزر فعلًا (stream/العملية في الواجهة) وما **لا** يستطيع إيقافه (دراسة خلفية على الخادم إن لم يوجد مسار إلغاء) — بلا تجميل.
6. **حدّث** STATUS_BOARD (صف S4) + التقرير بالنموذج.

## بوابة القبول
- `pnpm -C ui test` أخضر لملفات `p6` (سجّل العدد الحرفي).
- `pnpm -C ui typecheck` أخضر.
- سيناريو 8-4-و (بدء عملية → طوارئ → توقف) موثق خطوة بخطوة بما شوهد فعلًا.

## توقف
لا تبدأ S5. اعرض التقرير وانتظر التصريح.

# S3 — تدقيق أدلة الثقة (لا رقم بلا أمر)

> **الصق هذا الملف وحده للوكيل المنفذ. توقف عند التقرير.**

## هويتك وقوانينك
R1 كل رقم له أمر + مخرج حرفي. R3 الصدق > الجمال (أي رقم غير قابل للتكرار يُحذف أو يُستبدل بمقيس). R7 لا ملف خارج القائمة.

## الشك المؤكد (سبب المرحلة)
- `docs/VALIDATION_REPORT.md` يعلن `IEEE 1584 … 0.00% Exact Match` بينما اختبار ST نفسه يتسامح ±5% (تيار) و±15% (طاقة) ⇒ الرقم **غير قابل للتكرار** بصيغته.
- عبارة `CERTIFICATION STATUS: CERTIFIED PASS` ادعائية بلا جهة تصديق.
- أرقام أخرى (مثل 0.04% و17.4288 kA) مطلوب إثباتها بالأوامر المذكورة في نفس الوثيقة.

## الملفات المسموح لمسها حصريًا
`docs\VALIDATION_REPORT.md` · `docs\STANDARDS_COMPLIANCE_MATRIX.md` · `docs\TRUST_CHARTER.md` · `SECURITY.md` · `ui\src\components\cards\ResultCard.tsx` · `ui\src\__tests__\p9\import-export-chat.test.tsx` · `docs\AR\README.ar.md` · `docs\status\STATUS_BOARD.md`

## الخطوات
1. **شغّل الأدلة حرفيًا** وسجّل المخرجات:
   - `& .venv-fix/Scripts/python scripts/run_ieee_benchmarks.py`
   - `& .venv-fix/Scripts/python scripts/claims_audit.py --strict`
   - `& .venv-fix/Scripts/python -m pytest tests/test_arcflash_1584_st_cases.py tests/test_ieee_gold_standard_benchmarks.py -q`
2. **أعد بناء جدول VALIDATION_REPORT** من الأرقام المقاسة فقط + تاريخ + `git rev-parse HEAD`. أي رقم لا يظهر في مخرجات أمر: احذفه أو أعد قياسه. أزل `CERTIFIED PASS` واستبدلها بعبارة قابلة للإثبات (اسم الاختبار + التاريخ + الـcommit).
3. **TRUST_CHARTER (بند 7-3أ):** تحقق فعليًا هل كل رقم يمر بـMathGuard في مسار `study_executor → engine`. إن لم يكن، أعد صياغة الوعد بنطاقه الحقيقي (مثال: «أجوبة المحادثة عبر الـorchestrator») بلا تعميم.
4. **اللغة القانونية:** وحّد أي ذكر لـ«PE Stamp / Required by law» إلى الصياغة الآمنة: «Engineering Review Hash (SHA-256) + dual-approval — ليست ختم مهندس مرخّص، وتتطلب توقيع مهندس بشري». (أسماء الفئات لا تتغير.)
5. **SECURITY.md:** غلّف سطر تدوير المفاتيح بـ`[PENDING OWNER CONFIRMATION — 2026-09]` حتى تأكيد المالك (سلّم القرار للمستشار). لا تحذفه بلا إبلاغ.
6. **ResultCard/الاختبار المرافق:** العبارة الثابتة `16/16 Verified (claims_audit)` — اجعلها مربوطة بدليل قابل للتحديث (أو خفّض الصياغة إلى «قابل للتحقق عبر claims_audit»). وأزل أي إسناد غير مدعوم (مثال `data_export → ISO 27001`) من الخريطة.
7. **حدّث** STATUS_BOARD (صف S3) + التقرير بالنموذج.

## بوابة القبول
- كل رقم باقٍ في الوثائق الثلاث له أمر داخل نفس الوثيقة ونتيجته الحرفية في تقريرك.
- لا وجود لعبارات `Exact Match` / `CERTIFIED` غير مثبتة قابلة للتكرار.
- `pnpm -C ui test` يخص اختبارات الملفات المعدلة (إن لم يكن `pnpm` جاهزًا في بيئتك: أبلغني بدلًا من تخطي الاختبار).

## توقف
لا تبدأ S4. اعرض التقرير وانتظر التصريح.

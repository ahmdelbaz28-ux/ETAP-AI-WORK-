# S8 — التقرير النهائي + HF smoke

> **الصق هذا الملف وحده للوكيل المنفذ. توقف عند التقرير النهائي.**

## الملفات
`docs\status\FINAL_REPORT.md` (جديد) · `docs\status\STATUS_BOARD.md` (تحديث الصف S8).

## الخطوات
1. **جدول المرحلة 10 حرفيًا** (بند | قبل | بعد | الدليل «أمر + نتيجة حرفية») لكل بنود البرومبت الأصلية. أي صف لا تستطيع إثباته يُكتب فيه: **«غير منفذ + السبب + اقتراح الجولة القادمة»**. ممنوع تجميل رقم واحد.
2. أدرج على الأقل هذه الصفوف: BYOK/CORS · IEC 60909 method · أرقام الوثائق المصححة · الطوارئ · dispatch · حالة الالتزامات · CI على PR · SHA الدمج · الحالة الصحية للـHF Space.
3. **HF Space:** تابع البناء حتى `RUNNING` (ليس `RUNNING_BUILDING`)، ثم smoke حقيقي من المتصفح:
   أول شاشة = ChatWorkspace → محادثة → موافقة → تقدم حي → نتيجة. **سجّل ما رأيته حرفيًا** بلا وصف إنشائي؛ وإن تعذّر (بيئة/شبكة) اكتب ذلك صراحة.
4. احفظ `docs/status/FINAL_REPORT.md`، وحدّث `STATUS_BOARD` (صف S8 = ✅)، ثم اعرض الملخص النهائي وتوقف.

## بوابة القبول
- كل خلية في الجدول لها دليل أمر، أو اعتراف صريح «غير منفذ + السبب».
- لا عبارة واحدة في التقرير بلا سند.
- الشجرة نظيفة وHEAD معروف ومُعلن.

## توقف
هذه نهاية الجولة. اعرض التقرير النهائي وانتظر ختام المستشار.

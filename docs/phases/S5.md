# S5 — وكيل التصميم التوليدي: مواءمة dispatch وتوثيق (أو عزل بقرار المالك)

> **الصق هذا الملف وحده للوكيل المنفذ. توقف عند التقرير.**
> **توقف فورًا إن وصلتك تعليمات «عزل» من المستشار — العزل يُدار عبر PR عكسي موثق ولا تنفذه يدويًا.**

## هويتك وقوانينك
R1 لا ادعاء بلا أمر. R3 لا تجميل: الوكيل scaffold مبدئي وعلمه مغلق — هكذا يُوثَّق. R7 لا ملف خارج القائمة.

## حالة البداية المؤكدة
- الالتزام `7617d30be` أدخل: `agents/design_agent.py` + `StudyType.GENERATIVE_DESIGN` (`agents/models.py:62`) + تسجيل في `agents/registry.py` + `prompts/design_agent.prompt.yaml` + `prompts.json` + `tests/test_design_agent_scaffold.py` + علم `generative_design` بـ`enabled=False` في `api/feature_flags.py`.
- **عيب مؤكد من الكود:** `engine/dispatch.py` (منطقة 136-139) يبني الاستثناء `requires_system=False` على `(ETAP_EXPERT, ETAP_GUI)` فقط ⇒ سجل `generative_design` سيحمل `requires_system=True` وسيطالب ببيانات نظام لا يحتاجها الوكيل.
- الوكيل يفشل مغلقًا عند غياب الراية (`FAILED` + `flag_disabled`) — مغطى باختبارات قائمة.

## الملفات المسموح لمسها حصريًا
`engine\dispatch.py` · `tests\test_design_agent_scaffold.py` · `AGENTS.md` · `agents\design_agent.py` (توثيق فقط — لا منطق) · `docs\status\STATUS_BOARD.md`

## الخطوات (مسار الإكمال — الافتراضي)
1. أصلح استثناء `requires_system` في `_build_dispatch()` ليشمل `StudyType.GENERATIVE_DESIGN`.
2. أضف اختبارًا: `STUDY_DISPATCH["generative_design"].handler_type == "agent"` و`requires_system is False` (وأضف تحققًا مماثلًا لـ`etap_expert`/`etap_gui` إن لم يكن موجودًا).
3. أعد تشغيل اختبارات الوكيل القائمة وأثبت أن الرفض عند العلم المغلق ما زال `FAILED` صريحًا.
4. وثّق في `AGENTS.md`: الوكيل + العلم (مغلق افتراضيًا) + حدوده (scaffold مبدئي، مخرجات تصميم غير مُتحقق منها ميدانيًا) بصدق كامل.
5. **مسار العزل (فقط بقرار مكتوب من المستشار):** توقف ولا تنفذ؛ سيُدار عبر فرع/PR موثق من المستشار.
6. **حدّث** STATUS_BOARD (صف S5) + التقرير بالنموذج.

## إغلاق F-BASE-B (إلزامي ضمن هذه المرحلة)
ثلاثة إخفاقات ثابتة سببها الالتزام `7617d30be` (وكيل التصميم) — تُغلق هنا:
1. `tests/test_agent_registration_regression.py::test_no_orphan_agents_in_orchestrator` → `Unknown agents registered in orchestrator: ['generative_design']`
2. `tests/test_prompt_integration.py::TestPromptHandleMapping::test_all_prompts_mapped` → `Unmapped prompts: ['design_agent']`
3. `tests/unit_tests.py::TestMultiAgentCoordination::test_study_type_enum` → `assert 17 == 16`

**التوجيه:** مسار الإكمال (أ) → حدّث توقعات الاختبارات الثلاثة لتشمل `generative_design` / `design_agent` (17 قيمة) مع توثيق السبب. مسار العزل (ب) → تزول الثلاثة تلقائيًا مع إرجاع الالتزام بالـPR العكسي (لا تعديل عليها).

## بوابة القبول
- `& .venv-fix/Scripts/python -m pytest tests/test_design_agent_scaffold.py -q` أخضر (سجّل العدد والزمن — هذا الملف تجاوز 30 ثانية في بيئة التحقق، فسجّل زمنه الحقيقي ولا تتخطَّه).
- دليل حرفي على `requires_system is False` بعد الإصلاح.
- `ruff check engine/dispatch.py` نظيف + `AGENTS.md` محدث.

## ملاحظات قياس
- تشغيل هذا الملف منفردًا تجاوز حد أداة المستشار (30s) — على الأرجح بسبب استيرادات `agents.registry` الثقيلة. إن تجاوز 90s في بيئتك: أبلغني بالزمن ولا تجعله يمر صامتًا.

## توقف
لا تبدأ S6. اعرض التقرير وانتظر التصريح.

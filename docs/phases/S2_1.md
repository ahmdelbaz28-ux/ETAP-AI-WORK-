# S2.1 — تصحيح مصغر: إزالة بندَين معياريين غير مُسنَدين داخل المستودع (R3)

**النوع:** تصحيح توثيقي محدود جدًا (سطران) — لا تغيير كود ولا اختبارات.
**المدة المتوقعة:** دقائق.

---

## 1) السياق — نتائج تدقيق المستشار على S2

اعتمد المستشار معظم S2 (توثيق + اختبارات أدلة ✅)، **وتحقّق بنفسه** أن:
- بنود `Clause 3.6.1` (KG) و`Clause 3.3.3` (KT) و`Clause 4.3.3.1` (R/X) و`Clause 4.3.1.2` (kappa) **موجودة سابقًا في الكود نفسه** (راجع `git show HEAD:fault_analysis/iec60909_engine.py`) ⇒ استخدامك لها سليم ومُسنَد.
- تعديلات المحرك **توثيقية بالكامل** (4 hunks كلها docstrings: 73+/14−، صفر تغيير منطقي).
- ملف الاختبار **إضافة خالصة** (+123/−0) — الأحد عشر اختبارًا المنشورة لم تُمَس.
- قيم الثبات (KG=1.019447 · KT=0.974813 · KU=1.019937/0.919262) مطابقة للصيغ المرجعية الموجودة أصلًا في الملف نفسه.

## 2) المشكلة المتبقية الوحيدة (مخالفة R3)

| # | الموضع | الادعاء المُضاف | لماذا مرفوض |
|---|---|---|---|
| A | `calculate_ku` docstring — السطور الجديدة `Clause 3.7.1` و`Clause 3.7.2` | تقسيم البند 3.7 إلى بندين فرعيين | **لا يوجد أي مصدر داخل المستودع** لهذين الرقمين (الأصل كان يقول `Clause 3.7` فقط — وهو المُسنَد). ممنوع إضافة رقم بند من الذاكرة |
| B | `_calculate_kappa` docstring — فقرة `Method Classification` | وصف Method B بأنها «equivalent frequency fg» وMethod C بأنها «equivalent R/X ratio of individual mesh branches» | **لا مصدر داخل المستودع** لهذين الوصفين، والمسح الشامل أثبت عدم وجود أي ادعاء Method B/C في المستودع أصلًا. الوصف غير قابل للتحقق ⇒ لا يُكتب |

---

## 3) التصحيح المطلوب حرفيًا (ملف واحد فقط)

**الملف:** `fault_analysis/iec60909_engine.py` — **لا ملف غيره**. ممنوع لمس الاختبارات أو أي ملف آخر.

**أ) في docstring `calculate_ku`:**
- احذف رقمي البندين الفرعيين وأعد الصياغة المُسنَدة (كما في الأصل):
  - بدل `* Clause 3.7.1: Unit blocks WITHOUT on-load tap-changer:` ⇒ `* Unit blocks WITHOUT on-load tap-changer (Clause 3.7):`
  - بدل `* Clause 3.7.2: Unit blocks WITH on-load tap-changer:` ⇒ `* Unit blocks WITH on-load tap-changer (Clause 3.7):`
- أبقِ كل ما تبقى كما هو (بما فيه الإشارات إلى 3.6.1 و3.3.3 — فهي مُسنَدة من دوال KG/KT نفسها).

**ب) في docstring `_calculate_kappa`:**
- احذف فقرة `Method Classification:` بالكامل واستبدلها بحرفية:
  ```
  Method Scope:
  This engine implements Method A (Uniform ratio R/X, Clause 4.3.1.2) only.
  Methods B and C are not implemented in this engine.
  ```
- ممنوع وصف Method B أو Method C بأي شكل (لا اسم، لا صيغة، لا حرف رمز).

---

## 4) بوابة القبول

| الشرط | الدليل |
|---|---|
| `pytest tests/test_iec60909_published_cases.py -q -o addopts='' -p no:cacheprovider` | 15 passed (سجّل الزمن الحرفي) |
| `ruff check fault_analysis/iec60909_engine.py --config ruff.toml` + `ruff check .` | `All checks passed!` |
| `git diff --stat` | لا يظهر أي ملف غير `fault_analysis/iec60909_engine.py` (بالإضافة إلى ملفات المراحل السابقة غير الملتزمة) |
| `git grep -n "3.7.1\|3.7.2" -- fault_analysis` | **صفر نتائج** |
| `git grep -n "equivalent frequency\|method c\|Method C\|Method B" -- fault_analysis` | صفر نتائج (أو مشار إليها بالمصدر إن وُجد) |
| HEAD | ثابت `c7cf56e6c` |

---

## 5) التقرير ثم التوقف

تقرير مختصر بنموذج `docs/status/REPORT_TEMPLATE.md` (يكفي ملء الأقسام المتعلقة) + تحديث صف `S2.1` في `STATUS_BOARD`، ثم توقف وانتظر تصريح S3.

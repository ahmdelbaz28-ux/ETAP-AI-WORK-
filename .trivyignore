# Trivy ignore list — one CVE ID per line.
# Lines starting with # are comments.
#
# All CVEs listed here are in DEV dependencies that are NOT shipped to
# production. Production deploys include only the Vite-built dist/ assets.
#
# Review quarterly — remove entries when the underlying package is upgraded.

# ─── react-router 7.x CVEs (SSR-only, no SSR in production) ────────────────
CVE-2025-43865
CVE-2025-59057
CVE-2026-21884
CVE-2026-22029
CVE-2026-34077
CVE-2026-42211
CVE-2026-42342

# ─── turbo-stream 2.4.0 (transitive dep of react-router, dev-only) ─────────
# CVE-2026-34077 already listed above (same CVE, different package)

# ─── vitest 2.1.8 CVEs (dev-only, vitest UI server never runs in prod) ─────
CVE-2025-24964
CVE-2026-47429

# ─── vite 5.x / 6.0.5 CVEs (dev server only, not in production build) ──────
CVE-2026-39363
CVE-2026-53571

# ─── undici CVEs (transitive dep of Node.js, not directly exploitable) ─────
CVE-2026-6734
CVE-2026-9697
CVE-2026-12151

# ─── protobufjs CVEs (transitive dep of @mastra/*, dev-only) ───────────────
CVE-2026-44289
CVE-2026-44290
CVE-2026-44291
CVE-2026-44293
CVE-2026-48712

# ─── rollup CVEs (transitive dep of vite, dev-only) ────────────────────────
CVE-2026-27606

# ─── hono CVEs (transitive dep of @mastra/*, dev-only) ─────────────────────
CVE-2026-54290

# ─── langsmith CVEs (transitive dep of @mastra/*, dev-only) ────────────────
CVE-2026-45134

# ─── protobufjs (CRITICAL) — transitive dep of @mastra/*, dev-only ─────────
CVE-2026-41242

# ─── undici CVE-2026-12143 (transitive dep, dev-only) ──────────────────────
CVE-2026-12143

# ─── @opentelemetry/exporter-prometheus (transitive dep, dev-only) ─────────
CVE-2026-44902

# ─── Python CVEs (transitive deps, not directly exploitable in our context) ──
# These are in Python packages that are either dev-only or not exposed to
# untrusted input in our deployment. Review quarterly.

# ─── numpy CVEs (used for numerical computation, not security-critical) ────
CVE-2024-47700
CVE-2024-47701

# ─── pillow CVEs (image processing, not exposed to untrusted input) ────────
CVE-2024-47774
CVE-2025-27497
CVE-2025-48379

# ─── cryptography CVEs (TLS library, not directly exploitable) ─────────────
CVE-2024-12797

# ─── urllib3 CVEs (transitive dep of requests/botocore) ────────────────────
CVE-2025-0822
CVE-2025-5078

# ─── requests CVEs (HTTP client, not exposed to untrusted input) ───────────
CVE-2024-47081

# ─── starlette CVEs (ASGI framework, mitigated by our middleware) ──────────
CVE-2024-47874
CVE-2025-54121

# ─── pypdf CVEs (PDF processing, not exposed to untrusted input) ───────────
CVE-2025-26466
CVE-2025-27116

# ─── fast-uri CVEs (transitive dep of ajv in ui dev toolchain, dev-only) ────
CVE-2026-75899
CVE-2026-75931
CVE-2026-75975
CVE-2026-76172

